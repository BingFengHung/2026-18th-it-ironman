# Day 23：端到端實戰啟動（Windows AI 自動化系統架構）

> 前面 22 天分別完成了檔案分類、RCA 分析、Event 日誌處理、工作日報、剪貼簿、Subflow、HITL、Semaphore 與快取等元件。
> 再來要處理的是整合問題：如何讓這些流程有清楚的資料邊界、錯誤處理與資源控制。接下來的幾天會完成端到端整合範例，逐層補上蒐集、決策、執行、審計與韌性處理。

---

本文同步發布於 GitHub： [2026-18th-it-ironman](https://github.com/BingFengHung/2026-18th-it-ironman/blob/main/Day23_端到端實戰啟動_Windows智慧管家總架構規格.md)

## 五層系統架構

| 架構層級                                  | 模組名稱           | 核心職責                                                                                             | 關鍵技術與節點                                                                                                               |
| :---------------------------------------- | :----------------- | :--------------------------------------------------------------------------------------------------- | :--------------------------------------------------------------------------------------------------------------------------- |
| **第 1 層：採集層（Ingestion）**    | 多源現場感知器     | 檔案系統監聽與變更捕獲；記憶體與 CPU 零開銷巡檢；Windows Event Viewer 崩潰日誌提取                   | Node.js`fs.watch` / `readdirSync`；`os.totalmem()` / `freemem()`；PowerShell `Get-WinEvent`                        |
| **第 2 層：護欄層（Guardrails）**   | 效能與安全閘門     | 3000 字肥大日誌前置瘦身；重複報錯零延遲回應；限制 AI 並行數，避免 CPU 飆高                           | **JSONata 表達式**（Change 節點）；**MD5 特徵快取**（Flow Context 快取）；**Semaphore 號誌鎖**（狀態計數） |
| **第 3 層：決策層（AI Brain）**     | agy 多任務推理大腦 | 語意意圖分流；嚴格 JSON 格式保證與結構化輸出；雙軌解包容錯防禦                                       | Google`agy CLI`；**`--json-schema` 契約約束**；Dual-Track Unwrap 解析器                                            |
| **第 4 層：執行層（Actions）**      | 多管道安全落地     | 檔案智慧歸檔與目錄建立；高危操作 Human-in-the-loop 人工審批；隔離區搬移；Windows 原生 Toast 彈窗反饋 | `fs.renameSync` / `mkdirSync`；**HITL 審批門**；`%USERPROFILE%\.quarantine` 隔離區；WinRT Toast API              |
| **第 5 層：審計層（Audit & Logs）** | 數據持久化與自癒   | 全生命週期 Audit Trail 存證追蹤；全域 Catch 錯誤捕獲與指數退避重試；本機規則庫 Fallback 降級保底     | `SystemLogs/audit_trail.log`；Exponential Backoff（1s ➔ 2s ➔ 4s）；Catch 節點與 Local Fallback 字典                      |

---

## 系統總架構設計圖

![系統架構流程圖](image/day23/01.png)

這張圖的閱讀方式是由左到右看資料如何被加工，再由下方回到審計與自癒層：採集層只提供事實，護欄層決定是否值得進入 AI，決策層產生結構化建議，執行層才有權改變檔案或發送通知。

今天的重點不是立即把五層全部塞進一個巨大 Flow，而是先定義每一層的責任與交界。後續未來實作時，只新增一層，才能在出錯時知道問題發生在哪個邊界。

---

## 資料流通訊協議（Universal Task Envelope）

在各層之間，我們採用標準化的 **Envelope 資料包** 進行流轉，確保跨模組溝通的強健性：

```json
{
  "taskId": "task_e2e_1724389200000",
  "source": "DOWNLOADS_WATCHER",
  "task_type": "CLASSIFY_FILE",
  "timestamp": "2026-08-23T10:30:00.000Z",
  "payload": {
    "target": "Docker_Desktop_Installer_v4.30.exe",
    "file_size_mb": 580,
    "metrics": {
      "mem_percent": 68,
      "cpu_load": "NORMAL"
    }
  },
  "guardrails": {
    "cached": false,
    "concurrency_locked": true,
    "ttl_ms": 3600000
  },
  "decision": null,
  "execution_status": "PENDING"
}
```

---

## E2E 模組職責劃分與邊界約束

```text
┌──────────────────────────────────────────────────────────────────────────┐
│                      Windows AI 智慧管家 E2E 系統                         │
├──────────────────────────────────────────────────────────────────────────┤
│ [1. 採集層] ──(原始事件)──> [2. 護欄層] ──(乾淨任務)──> [3. 決策大腦]     │
│                                 │                            │           │
│                           (0ms 快取命中)                (結構化決策)     │
│                                 ▼                            ▼           │
│ [5. 審計與自癒] <──(審計記錄)── [4. 安全執行與審批門] <───────┘           │
└──────────────────────────────────────────────────────────────────────────┘
```

採集層與護欄層之間的邊界，是採集層只負責收集事實，絕不直接調用 AI；所有輸入必須先通過 JSONata 瘦身與快取查詢。

決策大腦與執行層之間的邊界，是大腦只負責給出結構化建議，絕不直接下達物理刪除指令；所有寫入操作由執行層的安全審批門接管。

執行層與審計層之間的邊界，則是任何一筆執行動作都必須同步寫入本地審計庫，保證 100% 可追溯。

### 這份規格如何被驗收？

可以用一筆「下載區檔案分類」任務走完整流程：採集層產生事件、護欄層標記快取與並行狀態、決策層回傳分類結果、執行層完成安全搬移，最後審計層留下任務 ID、結果與錯誤資訊。任何一層若直接跳過自己的責任，都應視為架構違規，而不是靠下一層猜測補救。

`Universal Task Envelope` 的價值也在這裡：它讓每一層都能讀取固定欄位，不必知道前一層的內部實作；未來替換採集器、模型或通知管道時，資料契約仍可維持穩定。

---

## 接下來 5 天實戰路線圖

Day 24 會從實戰層 1 開始，完成雙軌資料採集與 JSONata 前置清洗標準化；Day 25 接著建立 `agy CLI` 多任務路由大腦與 Schema 約束；Day 26 處理多管道推播、Quarantine 隔離歸檔與 SQLite 審計；Day 27 加入 Semaphore 號誌鎖與 MD5 特徵快取；Day 28 最後完成全域自我修復、指數退避重試與 Fallback 降級保底。

接下來從資料採集與前置清洗開始。

---

## 為什麼要先定義總架構？

端到端流程最容易出現的問題，不是單一節點不會運作，而是每個節點都各自假設資料格式。例如採集層可能把檔案路徑放在 `msg.payload`，AI 層卻期待 `msg.input.file_path`，通知層又直接讀取 `msg.result`。這種做法在單獨測試時看似正常，一旦把多個 Flow 串在一起，就會產生欄位遺失、型別不一致與錯誤難以追蹤的問題。因此， Day 23 的重點不是增加更多節點，而是先決定每一層都必須遵守的共同語言。

### 架構規格需要回答的問題

這份架構規格首先要說清楚一筆任務從哪裡開始，也要界定每一層可以讀取與修改哪些欄位。除此之外，AI 輸出的資料必須有明確的交接方式，讓下一層可以安全接收；發生錯誤時，也要能知道由哪一層負責重試、哪一層負責通知。最後不能只看節點是否有輸出，而要確認任務是否真的完成並留下可追查的結果。

如果這五個問題沒有答案，後續的 Flow 即使可以匯入，也很難稱為可維護的系統。

## Universal Task Envelope 欄位說明

Envelope 的目的，是讓所有層使用相同的任務外殼，並將每一層的專屬資料放在明確的區域。

| 欄位          | 用途               | 主要寫入者 | 下游用途                 |
| ------------- | ------------------ | ---------- | ------------------------ |
| `task_id`   | 識別單一任務       | 入口層     | 日誌、審計、重試關聯     |
| `task_type` | 表示任務種類       | 路由器     | 選擇 Prompt 與 Schema    |
| `source`    | 記錄事件來源       | 採集層     | 追蹤排程、手動或檔案事件 |
| `input`     | 保留清洗後輸入     | 採集層     | AI 推理與規則判斷        |
| `decision`  | 保存 AI 結構化結果 | AI 層      | 執行與通知               |
| `audit`     | 記錄處理歷程       | 審計層     | 追查與統計               |
| `retry`     | 保存重試狀態       | 韌性層     | 退避與停止條件           |

欄位命名固定後，Function 節點不需要猜測上一個節點使用了什麼名稱。

### 必填欄位與選填欄位

入口建立任務時，至少要有 `task_id`、`task_type`、`source` 與 `input`。`decision`、`audit` 與 `retry` 可以在流程中逐步建立，但不能以不明確的字串取代物件。

例如，沒有決策結果時，應使用 `decision: null` 或根本不建立欄位，而不是填入「尚未完成」這種無法判斷的文字。這樣可以讓下游用型別檢查判斷狀態，也能避免通知層把錯誤訊息誤當成正常結果。

## 一筆任務的生命週期

一筆任務進入系統後，首先會建立唯一的 `task_id`，並記錄來源與建立時間。接著由採集層把原始資料整理成可被後續處理的格式，但不在這裡做 AI 判斷。資料進入路由器後，系統依照 `task_type` 選擇 Prompt、Schema 與執行策略，再由共用 AI 元件負責推理與結構化回傳。完成決策後，執行層才依照結果進行有限且可追蹤的副作用操作。最後流程會寫入審計結果、釋放資源，並在需要時發送通知。

這個生命週期讓每個階段都能單獨測試，也能在任一階段發生錯誤時保留足夠上下文。

## 各層之間的責任邊界

**採集層**不能直接呼叫 AI，否則資料清洗與推理責任會混在一起。**AI 層**不能直接操作檔案系統，否則模型輸出會直接變成不可逆的副作用。**執行層**不能自行修改任務類型，否則審計紀錄會失去原始語意。**審計層**應盡量記錄事實，不應重新推測 AI 為什麼做出某個決定。**韌性層**則要判斷錯誤是否值得重試，而不是對所有錯誤無限重跑。這些限制看似增加程式碼，但能降低日後替換元件時的影響範圍。

## 驗收前的最小測試案例

在進入 Day 24 之前，至少要準備三種輸入：正常任務、缺少欄位的任務，以及格式錯誤的任務。

正常任務應能走完整生命週期並產生審計紀錄。缺少 `task_type` 時，應在路由前被拒絕，而不是交給 AI 猜測。格式錯誤時，應保留 `task_id` 與錯誤原因，讓後續可以追查。另外還要測試兩個任務同時進入時，是否仍能各自保留正確的 `task_id`。這些案例會成為 Day 24～28 的共同驗收基準。後續實作會沿用這套敘事與契約，讓每一天的新增功能都能回到同一條端到端資料流中理解。

## 今日總結與明日預告

今天的重點是把接下來五天的整合邊界與資料契約定義清楚，讓後續每一層都能獨立實作、測試與替換，以此來作為後續實作的架構規格與驗收基準。

明天將從採集層開始，建立雙軌資料採集與前置清洗流程。
